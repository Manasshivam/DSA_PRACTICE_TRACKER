class Solution:
    def largestElement(self, nums):
        max_num =  nums[0]
        for num in nums:
            if num > max_num:
                max_num = num
            
            return max_num
